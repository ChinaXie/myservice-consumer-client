spring:
  application:
    name: cloud-demo
  cloud:
    config:
      #uri: http://localhost:8888/
      profile: dev
      discovery:
        enabled: true
        service-id: microservice-configs-server
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/